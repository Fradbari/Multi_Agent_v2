apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: crewai-outputs-pvc
  namespace: multi-agent-ecosystem
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi